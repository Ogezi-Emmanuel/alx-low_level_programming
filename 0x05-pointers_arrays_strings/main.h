#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

/**
 * main - the main function of the prototypes
 * void prototype (void)
 * int prototype (int)
 */

void reset_to_98(int *n);
void swap_int(int *a, int *b);
int _strlen(char *s);
void _puts(char *str);

#endif
