#include "main.h"

/**
 * _abs - prints the absolute value of an intiger
 * @n: number to be computed
 * Return: the absolute value of n
 */

int _abs(int n)
{
	if (n > 0);
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
